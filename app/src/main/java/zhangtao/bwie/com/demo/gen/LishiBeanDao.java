package zhangtao.bwie.com.demo.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import Bean.LishiBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LISHI_BEAN".
*/
public class LishiBeanDao extends AbstractDao<LishiBean, Long> {

    public static final String TABLENAME = "LISHI_BEAN";

    /**
     * Properties of entity LishiBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Lid = new Property(0, Long.class, "lid", true, "_id");
        public final static Property Key = new Property(1, String.class, "key", false, "KEY");
        public final static Property Icons = new Property(2, String.class, "icons", false, "ICONS");
        public final static Property Titles = new Property(3, String.class, "titles", false, "TITLES");
    };


    public LishiBeanDao(DaoConfig config) {
        super(config);
    }
    
    public LishiBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LISHI_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: lid
                "\"KEY\" TEXT," + // 1: key
                "\"ICONS\" TEXT," + // 2: icons
                "\"TITLES\" TEXT);"); // 3: titles
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LISHI_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LishiBean entity) {
        stmt.clearBindings();
 
        Long lid = entity.getLid();
        if (lid != null) {
            stmt.bindLong(1, lid);
        }
 
        String key = entity.getKey();
        if (key != null) {
            stmt.bindString(2, key);
        }
 
        String icons = entity.getIcons();
        if (icons != null) {
            stmt.bindString(3, icons);
        }
 
        String titles = entity.getTitles();
        if (titles != null) {
            stmt.bindString(4, titles);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LishiBean entity) {
        stmt.clearBindings();
 
        Long lid = entity.getLid();
        if (lid != null) {
            stmt.bindLong(1, lid);
        }
 
        String key = entity.getKey();
        if (key != null) {
            stmt.bindString(2, key);
        }
 
        String icons = entity.getIcons();
        if (icons != null) {
            stmt.bindString(3, icons);
        }
 
        String titles = entity.getTitles();
        if (titles != null) {
            stmt.bindString(4, titles);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LishiBean readEntity(Cursor cursor, int offset) {
        LishiBean entity = new LishiBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // lid
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // key
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // icons
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // titles
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LishiBean entity, int offset) {
        entity.setLid(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setKey(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIcons(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTitles(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LishiBean entity, long rowId) {
        entity.setLid(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LishiBean entity) {
        if(entity != null) {
            return entity.getLid();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
